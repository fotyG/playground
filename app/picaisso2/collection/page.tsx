// "use client";

// import { useQuery } from "convex/react";

// const CollectionPage = () => {
//   const allSketches = useQuery<any>("sketches:getSketches");

//   return (
//     <div className="container">
//       {allSketches?.map((sketch: any) => (
//         <p key={sketch._id}>{sketch.prompt}</p>
//       ))}
//     </div>
//   );
// };
// export default CollectionPage;
